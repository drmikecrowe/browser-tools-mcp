/**
 * Setup message handlers for the background script
 */
export function setupMessageHandlers() {
  console.log("Background: Setting up message handlers")

  // Register Plasmo message handlers
  // These are automatically handled by the @plasmohq/messaging system
  // The actual handlers are in the messages/ directory
}
